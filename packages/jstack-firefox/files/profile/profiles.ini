; jstack OS default Firefox profile. A fixed profile directory name lets the
; distro ship user.js and chrome/userChrome.css via /etc/skel. Firefox claims
; this profile on first launch and records its install hash here.
[General]
StartWithLastProfile=1
Version=2

[Profile0]
Name=default
IsRelative=1
Path=jstack.default
Default=1
