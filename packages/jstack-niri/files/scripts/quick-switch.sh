#!/bin/bash
# Quick window switcher for niri - switches immediately without visual switcher

# For now, just switch to the previously focused window
# This provides instant Alt+Tab switching
niri msg action focus-window-previous